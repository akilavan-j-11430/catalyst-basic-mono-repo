# No app-specific variables. Shared Catalyst vars live in the root .env.

# Optional. Injected by `catalyst serve`; set only to pin the port for a
# standalone `pnpm --filter proxy dev` run. Defaults to 3000.
X_ZOHO_CATALYST_LISTEN_PORT=
