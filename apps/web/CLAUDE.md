@AGENTS.md

# apps/web

Next.js 16 / React 19 / Tailwind 4 frontend. Deployed as the Catalyst Slate app `web`.

Dev port: **4000** (`next dev -p 4000`).

## Reaching the API

Open the URL `catalyst serve` prints (first free port from 3000 up - often 3001), not
`:4000`. `apps/proxy` is the single origin: it forwards `/api/*` to the API on `:8000`
and everything else here. This app has **no rewrite or proxy config of its own**, so a
request made straight to `:4000/api/...` gets a Next.js 404, and one made to `:8000` is
cross-origin and missing the Catalyst headers the SDK needs.

Call it through `src/services/`, never `fetch` in a component:

```
src/services/<domain>.ts       the calls themselves (auth.ts, ...)
src/services/api/client.ts     transport - returns the whole response, like the server's HttpClient
src/services/api/endpoints.ts  every path this app calls
```

`api/` changes when the transport does; the domain files change when the product does.
Components import a domain file and never reach below it.

```ts
const { mutate } = useMutation({ mutationFn: registerUser });
```

`.claude/rules/web_data_access.md` is the full rule and loads automatically here - how to
add a call, how query keys are shaped, and what a call site is and is not responsible for.

Shapes both sides use live in `@repo/types` (`@repo/types/api` for the envelope,
`@repo/types/user` for the user shapes) - defined once, never redeclared here.

Note `packages/types` carries a `prepare` script on purpose. Slate builds this app with a
bare `pnpm install && pnpm run build` and never runs turbo, so without it `@repo/types`
has no `dist/` at build time and every import of it fails in the cloud while working
locally.

## Data fetching, forms and errors

Three libraries, each with one job. `@tanstack/react-query` owns server state,
`react-hook-form` owns form state, `sonner` renders the toasts - and
`.claude/rules/web_data_access.md` covers the first two in full.

What belongs here is the toast, because it is styling rather than data flow.
`components/ui/sonner.tsx` is the vendored Toaster, sitting in the centre of the page at
`--width: 75vw`. **Its whole look lives in `globals.css`, not in the component.** Sonner
sets `background` as a shorthand, which wipes out any `background-image` a utility class
adds, and styles `[data-icon]` three attributes deep - so Tailwind classes lose on
specificity whatever you do. Each rule there is one attribute wider than the one it
replaces, which is why it wins on specificity rather than on the load order of sonner's
runtime `<style>`. Read the note above that block before changing how a toast looks.

`QueryProvider` (`src/components/shared/query_provider.tsx`) is what connects the two:
mounted in the root layout, it wires `onError` on both caches to a toast, so no call site
writes error handling. Success messages stay at the call site - every failure has the same
shape and every success does not.

## Where components go

Three tiers under `src/components/`, and the split is about who owns the file:

```
src/components/ui/       shadcn primitives - vendored, regenerated by `shadcn add`
src/components/shared/   our own reusable pieces, no feature knowledge (loader, form_field)
src/components/auth/     components that know what signing in means
```

`ui/` is upstream code: it keeps shadcn's naming and is replaced wholesale on an
update, so nothing there should be hand-edited. Restyle through the tokens in
`globals.css` instead.

`shared/` is for pieces a second feature could use unchanged. A component that names
a domain concept does not belong here - it gets a folder beside `auth/`.

Non-component code stays out of `components/` **once it has a second caller or is a
seam**: `src/catalyst/auth_client.ts` is the only file that talks to the Catalyst SDK,
`src/services/` is the layer that talks to our own API. What one component alone uses -
a form's resolver, a formatter - stays in that component until something else needs it.
Routes stay thin and import from the rest.

## Deployment

Slate builds this app from `.catalyst/slate-config.toml` (`pnpm install`,
`pnpm run build`, output `.next`). That file is generated and managed by the Catalyst
CLI - **do not hand-edit it**. Deployed from the repo root via `pnpm deploy`.

## Notes

- `AGENTS.md` above is regenerated by `next dev`. Leave it in place and commit it with
  your work; it carries the Next.js 16 guidance that differs from older versions.
- Lint with `pnpm --filter web lint`. This is the only workspace with a lint script.
