{
  "name": "catalyst-basic-mono-repo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "concurrently -k -n serve,apps -c cyan,green \"pnpm run serve < /dev/null\" \"turbo run dev\"",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "bundle": "turbo run bundle",
    "serve": "turbo run bundle --filter=proxy && catalyst serve --only appsail:proxy",
    "deploy": "turbo run bundle && catalyst deploy --only appsail:api,slate:web"
  },
  "packageManager": "pnpm@10.28.1",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "concurrently": "^10.0.5",
    "esbuild": "^0.28.2",
    "tsc-alias": "^1.9.5",
    "tsx": "^4.23.15",
    "turbo": "^2.11.2",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/node": "^26.6.2"
  }
}
